import { ScrolleffectsDirective } from './scrolleffects.directive';

describe('ScrolleffectsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolleffectsDirective();
    expect(directive).toBeTruthy();
  });
});
