import { HomescrollDirective } from './homescroll.directive';

describe('HomescrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HomescrollDirective();
    expect(directive).toBeTruthy();
  });
});
